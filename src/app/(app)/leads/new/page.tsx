import { NewLeadForm } from "@/components/leads/phase16-forms"; import { requireRole } from "@/lib/auth/require-role";
export const metadata={title:"New Lead — Drapeworks CRM"}; export default async function Page(){await requireRole(["consultant","admin"]);return <main className="max-w-3xl mx-auto px-4 sm:px-6 py-6 sm:py-8"><h1 className="text-2xl font-bold mb-6">New lead</h1><NewLeadForm/></main>}
